import { Meteor } from 'meteor/meteor';
import '../imports/api/Users';
import '../imports/api/Sensors';
import '../imports/api/Houses';
import '../imports/api/SensorReport';

Meteor.startup(() => {
  // code to run on server at startup
});
